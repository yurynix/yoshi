import React from 'react';
import styles from './style.sass';

export default () => (
  <div id="sass-auto-prefixer" className={styles['sass-auto-prefixer']}>
    Auto Prefixer is working!
  </div>
);
